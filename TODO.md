# TODO

- [x] refactor: extract the pure resolver logic from `neighborhood_locator.js` into a reusable module
- [x] feat: make the resolver callable as `匹配所属社区(地址): string | undefined`
- [x] feat: expose the resolver in a way other projects can consume directly, instead of having to import from the CLI script